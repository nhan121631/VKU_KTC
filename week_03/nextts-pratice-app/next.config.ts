import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
    images: {
    domains: [
      "i.imgur.com",
      "imgs.search.brave.com",
      "giftdev.ru",
      "i.dell.com",
      "upload.wikimedia.org",
      "placehold.co", 
      "i.dailymail.co.uk",
      "media.husqvarnagroup.com",

    ],
  },
};

export default nextConfig;
